prompt = """使用此工具来创建和管理待办事项（todo），以跟踪任务和进度。该工具提供全面的 todo 管理功能。

## 何时使用此工具

在以下情况下，应主动使用该工具：

1. **复杂的多步骤任务** – 任务需要 3 个或以上不同步骤或操作时
2. **非平凡且复杂的任务** – 任务需要仔细规划或执行多个操作
3. **用户明确请求 todo 列表** – 用户直接要求你使用待办列表
4. **用户提供多个任务** – 用户提供了一系列任务（编号或逗号分隔）
5. **收到新指示后** – 立即将用户需求捕捉为待办事项
6. **开始执行任务时** – 在开始工作前将其标记为 in_progress。理想情况下同时只应有一个待办事项处于 in_progress
7. **完成任务后** – 将其标记为 completed，并添加在执行过程中发现的任何新的后续任务

## 何时不使用此工具

在以下情况下，应跳过使用此工具：
1. 任务只有一个且非常简单
2. 任务琐碎，跟踪它不会带来任何组织上的益处
3. 任务可在不到 3 个简单步骤内完成
4. 任务纯粹是对话性质或仅提供信息

## 任务状态与管理

1. 任务状态（Task States）  
使用以下状态跟踪任务进度：
   - `pending`：任务尚未开始
   - `in_progress`：任务正在进行中（同一时间仅允许一个任务处于此状态）
   - `completed`：任务已成功完成

2. 任务管理（Task Management）
   - 在工作过程中实时更新任务状态
   - 完成任务后立即标记为 completed（不要批量处理完成任务） 
   - 同一时间只允许一个任务处于 in_progress 
   - 先完成当前任务，再开始新任务 
   - 对不再相关的任务，应从列表中完全移除

3. 任务完成要求（Task Completion Requirements）
   - **仅在完全完成任务时**标记为 `completed`
   - 若遇到错误、阻塞或无法完成，保持任务为 `in_progress`
   - 当任务被阻塞时，创建新任务描述需要解决的问题
   - 以下情况绝不标记为 `completed`：
     - 测试未通过
     - 实现不完整
     - 遇到未解决的错误
     - 找不到必要的文件或依赖

4. 任务拆分（Task Breakdown）
   - 创建具体且可执行的子任务
   - 将复杂任务拆分为更小、易管理的步骤
   - 使用清晰、描述性强的任务名称

5. 任务优先级（Task Priority）
   - 相同状态内的任务，按照优先级从高到低执行和展示任务列表
   - 禁止存在高优先级的任务依赖低优先级任务，这会导致任务的执行和展示顺序错乱

## 工具功能（Tool Capabilities）

- **创建新待办（Create new todos）**：添加任务，设置内容、优先级和状态
- **更新现有待办（Update existing todos）**：修改待办的任意属性（状态、优先级、内容）
- **删除待办（Delete todos）**：移除已完成或不再相关的任务 
- **批量操作（Batch operations）**：一次性更新多个待办 
- **清空所有待办（Clear all todos）**：重置整个待办列表

在不确定的情况下，请使用此工具。主动管理任务不仅体现细致认真，也能确保你成功完成所有要求。
"""