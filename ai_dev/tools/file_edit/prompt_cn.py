prompt: str =  f"""这是一个用于**编辑文件**的工具。  
若要**移动或重命名**文件，请使用 **BashExecuteTool** 并执行 `mv` 命令。  
若要进行**大规模编辑**，请使用 **FileWriteTool** 工具以覆盖整个文件。

## 使用此工具之前：
   1. 使用 FileReadTool 工具来了解文件内容及上下文。
   2. 验证目录路径是否正确（仅在创建新文件时适用）：
      - 使用 FileListTool 工具确认父目录是否存在且位置正确。

## 执行文件编辑时，应提供以下参数：
1. `file_path`：要修改的文件的绝对路径（必须是绝对路径，不能是相对路径）
2. `old_string`：要被替换的文本（必须在文件中唯一匹配，且与文件内容完全一致，包括空格和缩进）
3. `new_string`：用于替换 `old_string` 的新文本 
   > 工具将在指定文件中**仅替换一次** `old_string` 的出现。

## 关键要求（CRITICAL REQUIREMENTS）
1. 唯一性（UNIQUENESS）  
`old_string` **必须唯一的标识**你想修改的具体位置。这意味着：
   - 在修改点之前包含至少 3–5 行上下文；
   - 在修改点之后也包含至少 3–5 行上下文； 
   - **必须严格保留所有空格、缩进和代码格式**，完全与文件内容一致。
2. 单次修改（SINGLE INSTANCE）  
此工具一次只能修改一个实例。如果需要修改多个位置：
   - 为每个实例分别调用此工具； 
   - 每次调用都必须提供足够的上下文，确保唯一匹配目标位置。
3. 校验（VERIFICATION）  
在使用此工具前： 
   - 检查目标文本在文件中出现的次数； 
   - 若存在多个匹配实例，需提供足够上下文以区分它们； 
   - 为每个独立实例单独规划调用。

### 警告（WARNING）  
若未遵循上述要求：
- 若 old_string 匹配多个位置，工具将执行失败； 
- 若 old_string 不完全匹配（包括空格差异），工具将执行失败；
- 若上下文不足，可能会错误修改到其他位置。

### 编辑时的注意事项
- 确保编辑后代码语法正确、符合规范（idiomatic）；
- 不要让代码处于破坏状态； 
- 始终使用绝对路径（以 / 开头）。

### 创建新文件时
- 提供一个新的文件路径（如有需要包括目录名）；
- old_string 留空；
- new_string 为文件的完整内容。

### 多次编辑同一文件的建议
如果需要连续对同一文件进行多次编辑，建议在一条消息中包含多个此工具的调用，而不是发送多条消息、每条仅调用一次工具。"""